{
  "name": "Reputation",
  "type": "object",
  "title": "Reputation",
  "properties": {
    "did": {
      "type": "string",
      "description": "AT Protocol DID of the trader being rated"
    },
    "rater_did": {
      "type": "string",
      "description": "AT Protocol DID of the rater"
    },
    "rater_name": {
      "type": "string"
    },
    "rater_handle": {
      "type": "string"
    },
    "trade_uri": {
      "type": "string",
      "description": "at:// URI of the completed trade listing this rating covers"
    },
    "rating": {
      "type": "integer",
      "minimum": 1,
      "maximum": 5
    },
    "comment": {
      "type": "string",
      "maxLength": 280
    },
    "sig": {
      "type": "string",
      "description": "Signature binding the rating to the rater's signing key (simulated)"
    },
    "at_uri": {
      "type": "string"
    },
    "cid": {
      "type": "string",
      "description": "Content identifier hash of the federated record (simulated)"
    },
    "record_type": {
      "type": "string"
    },
    "bridged": {
      "type": "boolean",
      "default": false,
      "description": "True once the reputation record has been mirrored to the AT Protocol PDS as a real org.swappulse.tradingFeedback record, making it portable across PDSs and SwapPulse instances"
    },
    "content_hash": {
      "type": "string",
      "description": "SHA-256 of the canonical record body, refreshed on each bridge push. outbound-reconcile compares it against the rebuilt record to detect silent local drift and re-push."
    }
  },
  "required": [
    "did",
    "rater_did",
    "rating"
  ],
  "rls": {
    "read": {},
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}