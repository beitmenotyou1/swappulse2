{
  "lexicon": 1,
  "id": "org.swappulse.wishlist",
  "revision": 1,
  "description": "A card a collector wants to acquire, mirrored to AT Protocol as a real org.swappulse.wishlist record. Enables cross-instance trade matching.",
  "defs": {
    "main": {
      "type": "record",
      "key": "tid",
      "description": "A wishlist entry for a wanted card.",
      "record": {
        "type": "object",
        "required": ["cardUri", "cardName", "createdAt"],
        "properties": {
          "cardUri": { "type": "string", "description": "TCGDex card id of the wanted card" },
          "cardName": { "type": "string" },
          "imageUrl": { "type": "string", "format": "uri" },
          "setCode": { "type": "string", "description": "Canonical TCGDex set id" },
          "setName": { "type": "string" },
          "rarity": { "type": "string" },
          "maxPrice": { "type": "number", "description": "Max willing to pay in pence" },
          "ownerDid": { "type": "string", "format": "did", "description": "AT Protocol DID of the wishlist owner" },
          "createdAt": { "type": "string", "format": "datetime" }
        }
      }
    }
  }
}